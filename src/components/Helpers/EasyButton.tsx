import {StyleSheet} from 'react-native';
import React from 'react';

const EasyButton = ({mode}: EasyButtonProps) => {
  return <></>;
};

export default EasyButton;

const styles = StyleSheet.create({});

interface EasyButtonProps {
  mode: 'theme';
}
